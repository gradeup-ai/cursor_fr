import React from 'react';

interface VoiceControlProps {
  onCommand?: (command: string) => void;
}

const VoiceControl: React.FC<VoiceControlProps> = ({ onCommand }) => {
  return (
    <div>
      {/* Компонент будет реализован позже */}
    </div>
  );
};

export default VoiceControl; 